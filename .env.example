# Copy to .env on the production server and fill in real values.
# Generate a key with:  openssl rand -hex 32
SECRET_KEY=
ADMIN_USERNAME=aidy
# Only takes effect on FIRST start (admin user is created once).
ADMIN_PASSWORD=
